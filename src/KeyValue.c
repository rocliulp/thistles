/*
 * @author Paul
 */

#include <KeyValue.h>

struct KeyValue {
  byte key [KEY_CAPACITY];
  byte value [VALUE_CAPACITY];
};

int KeyValue_GetKey (byte key [], int len) {
}

int KeyValue_GetValue (byte value [], int len) {
}
